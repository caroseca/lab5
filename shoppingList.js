var myList = [
	{
		name: "banana",
		price: .5
	},
		name: "apple",
		price: 1

]
	name: ["banana", "apple", "dintyMooreBeefStew"],
	price: [0,1,2.25]
}

var printList = function(el) {
	console.log(el);
}

// myList.forEach.name(printList);

var total = myList.price[0] += myList.price[1] += myList.price[2];

console.log("Total is" + total);




myList.name.forEach(printList);